{
  "private": true,
  "name": "sloc-app",
  "description": "Source code line counter for Mac/Windows/Linux.",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git://github.com/stevenvachon/sloc-app.git"
  },
  "devDependencies": {
    "async": "~0.9",
    "bower": "^1.3",
    "chai": "^1.9",
    "fs-extra": "~0.11",
    "mocha": "^1.21",
    "node-webkit-builder": "~0.1",
    "steal-tools": "~0.4"
  },
  "scripts": {
    "build": "node build",
    "install": "bower install && cd src && npm install --production",
    "run": "nwbuild -r src/",
    "test": "mocha src/test/ --bail --reporter spec --check-leaks"
  }
}
