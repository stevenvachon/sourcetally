{
  "private": true,
  "name": "sourcetally",
  "description": "Source code line counter for Mac/Windows/Linux & web.",
  "version": "0.0.10",
  "repository": {
    "type": "git",
    "url": "git://github.com/stevenvachon/sourcetally.git"
  },
  "devDependencies": {
    "async": "~0.9",
    "bower": "^1.3",
    "fs-extra": "~0.12",
    "node-webkit-builder": "~0.1",
    "steal-tools": "~0.4"
  },
  "scripts": {
    "build": "node build 0.10.5",
    "install": "nwbuild --version 0.10.5 --forceDownload && bower install && cd src && npm install --production",
    "run": "nwbuild --version 0.10.5 --run src/",
    "test": "mocha src/test/ --bail --reporter spec --check-leaks"
  }
}
